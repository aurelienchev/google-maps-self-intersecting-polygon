{
  "name": "google-maps-self-intersecting-polygon",
  "version": "1.0.0",
  "description": "A Google Maps JavaScript API v3 library to validate self-intersecting an exisiting polygon.",
  "main": "Gruntfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "techievickyr@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-contrib-uglify": "^3.0.1"
  },
  "dependencies": {
    "imagemin-mozjpeg": "^5.1.0"
  }
}
